<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- AppCompat parent is required for per-app language switching on Android 12 and below. -->
    <style name="Theme.CheckPrint" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>
</resources>
